# =============================================================================
# spark-realtime-chatbot Dependencies (Docker Reference)
# =============================================================================
# These are the pinned versions from a known working configuration.
# The Dockerfile installs these packages; this file is for reference.
#
# Note: ctranslate2 is built from source in Docker, not installed via pip.
# =============================================================================

# -----------------------------------------------------------------------------
# Core Web Framework
# -----------------------------------------------------------------------------
fastapi>=0.124.0
uvicorn[standard]>=0.38.0
websockets>=15.0.1
aiohttp>=3.13.2
httpx>=0.28.1
python-multipart>=0.0.20
starlette>=0.50.0

# -----------------------------------------------------------------------------
# ASR (Speech-to-Text)
# -----------------------------------------------------------------------------
# ctranslate2 - built from source for Blackwell GPU support
faster-whisper>=1.2.1
tokenizers>=0.22.2
huggingface-hub>=0.36.0
onnxruntime>=1.23.2
av>=16.1.0

# -----------------------------------------------------------------------------
# TTS (Text-to-Speech) - Kokoro
# -----------------------------------------------------------------------------
kokoro>=0.9.4
kokoro-tts>=2.3.0
kokoro-onnx>=0.3.9
misaki>=0.9.4
phonemizer-fork>=3.3.1
soundfile>=0.13.1
scipy>=1.16.3
loguru>=0.7.3

# -----------------------------------------------------------------------------
# ML Frameworks
# -----------------------------------------------------------------------------
# Install PyTorch with CUDA 13.0:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
torch>=2.9.1
numpy>=2.2.6
transformers>=4.57.6
safetensors>=0.7.0

# -----------------------------------------------------------------------------
# Face Recognition (DeepFace)
# -----------------------------------------------------------------------------
deepface>=0.0.97
opencv-python>=4.12.0.88
tensorflow>=2.20.0
tf-keras>=2.20.1
mtcnn>=1.0.0
retina-face>=0.0.17
pillow>=12.0.0

# -----------------------------------------------------------------------------
# NLP (spaCy for text processing)
# -----------------------------------------------------------------------------
spacy>=3.8.11

# -----------------------------------------------------------------------------
# Code Execution Sandbox (run_python tool)
# -----------------------------------------------------------------------------
llm-sandbox>=0.3.31

# -----------------------------------------------------------------------------
# MCP client (for Hermes / future skill servers)
# -----------------------------------------------------------------------------
mcp>=1.10.0

# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
pydantic>=2.12.5
python-dotenv>=1.2.1
pandas>=2.3.3
tqdm>=4.67.1
regex>=2025.11.3
PyYAML>=6.0.3
requests>=2.32.5
